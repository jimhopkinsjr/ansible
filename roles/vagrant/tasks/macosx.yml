---
# roles/vagrant/tasks/macosx.yml

- name: "MacOS | Install packages."
  package:
    name: "{{ packages_macosx }}"
    state: latest

- name: "MacOS | Install Desktop apps."
  homebrew_cask:
    name: "{{ packages_macosx_casks }}"
    state: latest
