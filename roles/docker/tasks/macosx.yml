---
# roles/docker/tasks/macos.yml

- name: "MacOS | Package install."
  package:
    name:
      - docker
      - docker-compose
    state: latest
  tags:
  - packages

- name: "MacOS | Docker desktop app install"
  homebrew_cask:
    name: "docker"
    state: latest
  tags:
  - packages
