---
# roles/docker/tasks/main.yml

- name: "package install."
  package:
    name: "docker-engine"
    state: latest
  become: true
  tags:
  - packages

- name: "Enable service."
  service:
    name: "docker"
    state: "started"
    enabled: true
  become: true
  tags:
  - services

- name: "Add {{ username }} to docker group."
  user:
    name: "{{ username }}"
    groups: "docker"
    append: true
  become: true
  tags:
  - users
  - groups
